import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba5-consumo_api',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
